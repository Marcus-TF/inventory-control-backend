CREATE TABLE inventory.profile (
                                   profile_id uuid NOT NULL,
                                   profile varchar(255) NULL,
                                   CONSTRAINT profile_pkey PRIMARY KEY (profile_id)
);